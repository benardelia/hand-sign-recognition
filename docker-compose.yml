services:
  web:
    build: .
    container_name: handsign_app
    ports:
      - "5001:5001"
    volumes:
      - .:/app
    env_file:
      - .env
    command: gunicorn app:app --bind 0.0.0.0:5001 --workers 1 --threads 2 --timeout 120 --reload
